# Forecast-Pipeline (run.py) — Python 3.13, CPU-only.
# Installiert in forecast/.venv (nicht committet, siehe .gitignore).
# Verifizierte Versionen im bestehenden .venv (2026-07-24):
#   chronos-forecasting==2.3.1 (Modul: chronos, Chronos2Pipeline)
#   torch==2.13.0+cpu
#   pandas==3.0.5
#   numpy==2.5.1
#   requests==2.34.2
chronos-forecasting>=2.3.1
torch>=2.13.0
pandas>=3.0.5
numpy>=2.5.1
requests>=2.34.2
